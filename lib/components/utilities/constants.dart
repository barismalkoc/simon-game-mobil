import 'package:flutter/cupertino.dart';
const kAnimatedOpacityDuration = Duration(milliseconds: 200);
const kDelayedOpacityDuration = Duration(milliseconds: 200);

const kHeightSpacer = SizedBox(
  height: 30.0,
);
const kWidthSpacer =  SizedBox(
  width: 30.0,
);
enum OpacityColor {red, yellow, purple, grey, blue, green}